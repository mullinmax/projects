#include <iostream>
#include <vecotr>
#include <string>
using namespace std;

void load_tests(vector<string> &tests, vector<string> &expected);

int main(int argc, char *argv[]){
	vector<string> tests;
	vector<string> expected;
	load_tests(tests, expected);
	
}

void load_tests(vector<string> &tests, vector<string> &expected){
	
}
